<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.mscode.winsport">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name="com.mscode.winsport.Application"
        android:allowBackup="false"
        android:icon="@drawable/winsport_logo_icon"
        android:label="@string/app_name">

        <activity
            android:name="com.mscode.presentation.splashscreen.activity.WinSportActivity"
            android:theme="@style/Theme.WinSport"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>